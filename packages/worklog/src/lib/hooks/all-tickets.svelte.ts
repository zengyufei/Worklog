/**
 * Re-export from the unified tickets hook.
 *
 * @deprecated Import `getTickets` from `$lib/hooks/tickets.svelte` instead.
 *   `useAllTickets(path)` is equivalent to `getTickets(path)` (no board scope).
 */
export { useAllTickets } from './tickets.svelte';
